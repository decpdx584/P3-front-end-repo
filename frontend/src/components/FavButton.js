import React from 'react';
// use state to store true/false value isClicked for the user
// hold the logic there to increment/decrement.
const FavButton = (props) => {
  return(
    <button>Add to Favorites</button>
  )
}

export default FavButton;